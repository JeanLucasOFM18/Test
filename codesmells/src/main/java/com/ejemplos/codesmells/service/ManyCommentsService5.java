package com.ejemplos.codesmells.service;

public class ManyCommentsService5 {

    // This file contains a Java class with many comments
    // Each method is thoroughly documented to explain its functionality

    // The entry point of the program
    public static void main(String[] args) {
        // Declare a variable for age
        int age = 25; // Age of the user

        // Declare a variable for a person's name
        String name = "Alice"; // The user's name

        // Print a message using the age and name variables
        System.out.println("Name: " + name + ", Age: " + age); // Concatenate name and age and print it
    }

    // Method to check if the user is eligible to vote
    public boolean isEligibleToVote(int age) {
        // If age is greater than or equal to 18, they are eligible to vote
        if (age >= 18) {
            return true; // Eligible to vote
        } else {
            return false; // Not eligible to vote
        }
    }

    // Method to calculate the perimeter of a rectangle
    public int calculatePerimeter(int length, int width) {
        // Perimeter formula: 2 * (length + width)
        return 2 * (length + width); // Return the calculated perimeter
    }

    // Method to greet the user based on their name
    public void greetUser(String name) {
        // Display a greeting message with the name
        System.out.println("Hello, " + name + "! Welcome!"); // Greet the user by name
    }

    // Method to calculate the area of a rectangle
    public int calculateArea(int length, int width) {
        // Area formula: length * width
        return length * width; // Return the calculated area
    }

    // A simple method that returns a greeting message1
    // A simple method that returns a greeting message2
    // A simple method that returns a greeting message3
    // A simple method that returns a greeting message4
    // A simple method that returns a greeting message5
    // A simple method that returns a greeting message6
    // A simple method that returns a greeting message7
    // A simple method that returns a greeting message8
    // A simple method that returns a greeting message9
    // A simple method that returns a greeting message0
}
